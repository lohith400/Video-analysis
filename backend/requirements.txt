# Core inference stack
ultralytics>=8.4.0,<9.0.0
opencv-python>=4.8.0
easyocr>=1.7.0,<2.0.0
numpy>=1.24.0,<3.0.0
torch>=2.6.0
torchvision>=0.21.0
websockets>=12.0

# Web server (used by server.py but was missing from this file)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Testing (used by test_traffic_counter.py / test_model.py)
pytest>=8.0.0

# NOTE: torch/torchvision above install CPU wheels by default.
# For GPU (CUDA 12.4) install instead with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
