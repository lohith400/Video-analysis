# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# ── Virtual environments ───────────────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── ML Models & Weights (Ignore all large model files recursively) ─────────
*.pt
*.pth
*.onnx
*.weights
*.bin
yolov8n.pt
yolo26n.pt
yolo*.pt
# Keep models/ folder structure but ignore all weights inside it
!models/
models/*
!models/.gitkeep

# ── Datasets (Ignore entire dataset folders) ──────────────────────────────
plate_dataset/
dataset/
runs/

# ── Uploaded videos / images ───────────────────────────────────────────────
uploads/
*.mp4
*.avi
*.mov
*.mkv
*.jpg
*.jpeg
*.png
*.bmp

# ── Generated data / Logs ──────────────────────────────────────────────────
traffic_log.csv
*.csv
*.log
logs/
train_error.log

# ── Frontend build & deps ──────────────────────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/build/

# ── Zone.Identifier files (Windows WSL artefacts) ─────────────────────────
*Zone.Identifier
**/*Zone.Identifier
**/*:Zone.Identifier

# ── IDE / OS ───────────────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# Environment files (never commit real secrets)
.env
*.env
!.env.example

# Datasets, logs, archives and build artifacts — never commit
*.zip
ci_log*.zip
ci_log_extracted*/
indian-license-plates-with-labels.zip
plate_dataset_raw/
backend/plate_dataset_train/
*.timestamp-*.mjs
vehicle1.5_no_models.zip


