TASK: Merge the IRIS project upgrade into a new branch and get it running locally with existing trained model weights. The new branch must stay permanently separate from main — do not merge, rebase onto, or fast-forward main at any point, now or later.

CONTEXT
- Existing project: a local git clone of the "Video-analysis" repo (IRIS — Indian Road Intelligence System).
- Trained model weights already exist locally at backend/models/*.pt — DO NOT touch, overwrite, or delete anything in backend/models/ at any point in this task. Those are the only copies of the trained weights and are gitignored (not in the upgrade package).
- An upgrade package is available as a zip file named iris-upgraded.zip. It contains: Dockerfiles for backend and frontend, docker-compose.yml, a new auth.py module, refactored backend modules (schemas.py, plate_utils.py, helmet_logic.py, geometry_utils.py), new unit tests, .env.example files, an updated README.md, MODEL_CARD.md, LIMITATIONS.md, LICENSE, and a GitHub Actions CI workflow. It does NOT contain any .pt model weight files.

Locate both the existing repo directory and the iris-upgraded.zip file on this machine before proceeding. If either cannot be found, stop and ask me for the correct path instead of guessing.

STEP 1 — Create a new branch
In the existing repo, create and switch to a new branch called portfolio-upgrade, branched from the current default branch (main). Confirm the working tree is clean before branching; if there are uncommitted changes, stop and ask me how to proceed rather than discarding them. This branch is intended to live permanently alongside main as a separate, independent line of work — not a feature branch destined to be merged back.

STEP 2 — Merge the upgrade package
Extract iris-upgraded.zip to a temporary location, then copy its contents into the repo, with these rules:
- Copy every new/changed file from the zip into the matching path in the repo (Dockerfiles, docker-compose.yml, .github/workflows/ci.yml, all backend .py files including schemas.py, plate_utils.py, helmet_logic.py, geometry_utils.py, auth.py, the new test_*.py files, updated server.py, updated requirements.txt, updated pyproject.toml, .env.example files, frontend/src/api.js, updated frontend/src/pages/*.jsx, updated frontend/package.json, frontend/nginx.conf, README.md, MODEL_CARD.md, LIMITATIONS.md, LICENSE).
- DO NOT copy anything into backend/models/ — skip that directory entirely from the zip (it only contains a .gitkeep placeholder anyway, and the real weights already in the repo must not be touched).
- DO NOT copy the Notes/ folder unless it doesn't already exist in the repo.
- If a file exists in both the zip and the repo with different content, the zip version wins (it's the upgraded version) — but log every file you overwrite so I can review the diff afterward.

STEP 3 — Verify weights are in place and correctly named
Check that backend/models/ contains these exact filenames (these are what config.py expects):
- yolov8n.pt
- license_plate_detector.pt
- helmet_detector.pt
- gender_detector.pt
If any of these are missing or named differently, list what's actually present in backend/models/ and stop to ask me how to map existing files to the expected names — do not rename or guess.

STEP 4 — Create environment files
Run:
  cp backend/.env.example backend/.env
  cp frontend/.env.example frontend/.env
Leave IRIS_API_KEY blank in backend/.env (auth disabled, simplest local run). Leave VITE_API_URL at its default (http://localhost:8000) in frontend/.env.

STEP 5 — Verify the backend in isolation first
In backend/, create a fresh virtual environment, install requirements.txt, and confirm all Python files compile without syntax errors (python -m py_compile on every .py file). Then run the lightweight unit test suite:
  pytest test_traffic_counter.py test_plate_utils.py test_helmet_logic.py test_geometry_utils.py -v
All tests must pass. If any fail, stop and show me the failure output rather than trying to silently patch the source to make tests pass.

STEP 6 — Run it natively (primary path, since local CUDA/torch setup if any is already configured this way)
In one terminal:
  cd backend && source .venv/bin/activate && uvicorn server:app --host 0.0.0.0 --port 8000 --reload
In a second terminal:
  cd frontend && npm install && npm run dev
Wait for both to report they're listening, then confirm:
  curl http://localhost:8000/health
returns JSON with "models_loaded": true and "ocr_available": true. If either is false, capture the backend terminal's startup warnings (server.py logs which specific model failed to load and why) and show them to me.

STEP 7 — Optional: also verify the Docker path builds (does not need to replace Step 6, just confirm it works)
From the repo root:
  docker compose up --build
Confirm both containers report healthy and http://localhost:8000/health returns the same success response as Step 6. If Docker isn't installed or unavailable in this environment, skip this step and note that it wasn't verified.

STEP 8 — Commit on portfolio-upgrade and stop
Commit all the merged changes directly on the portfolio-upgrade branch (git add -A && git commit -m "Portfolio upgrade: Docker, auth, tests, docs"). Do NOT switch back to main, do NOT merge portfolio-upgrade into main, and do NOT open or suggest a pull request — main must remain completely untouched by this work. The two branches are meant to stay independent indefinitely, not converge later. Finish by giving me:
- A list of every file that was added, modified, or overwritten in this merge
- Confirmation that /health returned success in Step 6 (and Step 7 if run)
- Confirmation that all unit tests passed
- Confirmation that main was never checked out, modified, or merged into during this task
- Any warnings, errors, or ambiguous decisions you had to make along the way